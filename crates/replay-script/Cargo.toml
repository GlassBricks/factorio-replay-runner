[package]
name = "replay-script"
version = "0.1.0"
edition = "2024"

[dependencies]
itertools = "0.14.0"
serde = { version = "1.0.219", features = ["derive"] }
strum = { version = "0.27.1", features = ["derive"] }

[build-dependencies]
glob = "0.3"
